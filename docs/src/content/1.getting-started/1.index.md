---
title: Overview
description: Turn AI coding into an engineering process; agents and skills that enforce good software engineering.
navigation:
  icon: i-lucide-info
---

## Turn AI Coding Into an Engineering Process

Gem Team adds a coordinated engineering workflow to your AI coding tool:

- **Quality by default** — TDD, acceptance checks, and risk-based reviews and audits.
- **Anti-slop built in** — Quality Directives block dead buttons, buzzwords, template filler, and AI-generated noise.
- **Efficient execution** — 40-60% less context per task via scoped handoffs, evidence-by-reference, and high cache-hit rates.
- **Resumable plans** — persistent plan IDs and `plan.yaml` for MEDIUM/HIGH work; pause, resume, or extend without losing context.
- **Failure classification** — every failure is categorized (retry, fixable, replan, flaky, regression, platform-specific, test-bug) so it routes to the right agent.
- **Your tools, your choice** — Copilot, Claude, Cursor, Codex, Gemini, or Windsurf.
- **Continuous learning** — reusable decisions, patterns, and skills are retained.

Agent definitions and packaged skills provide the capabilities. The Orchestrator routes work, scopes context, and enforces verification gates.

## Match Models to Work

Configure two model tiers in `.gem-team.yaml`:

- **Premium** — planning, debugging, and review.
- **Explore** — research, implementation, testing, documentation, and other bounded work.

The workflow selects the configured tier for each delegated task.

## Workflow at a Glance

```text
Your Request
    ↓
Understand & Plan     ← Clarify intent, pick the right approach
    ↓
Execute & Verify      ← Build with TDD, verify every task, review risky boundaries
    ↓
Learn & Improve       ← Save patterns, skills, decisions for next time
    ↓
Done                  ← Status report + reusable knowledge
```
