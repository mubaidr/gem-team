---
title: Usage
description: Day-to-day patterns for working with Gem Team.
navigation:
  icon: i-lucide-play
---

## Start a Task

Describe the task. The Orchestrator gathers context, classifies complexity, plans, delegates, and verifies.

```text
"Add a rate limiter to the API gateway"
"Find out why the login flow fails on iOS Safari"
"Review the auth module for security issues"
```

## Complexity at a Glance

| Complexity  | When                                              | What You See                                                      |
| ----------- | ------------------------------------------------- | ----------------------------------------------------------------- |
| **TRIVIAL** | Typo fix, rename, one-shot command                | In-memory DAG and generated `execution_id`; no plan artifacts     |
| **LOW**     | Small, reversible, single-domain work             | Ephemeral DAG through the shared execution loop                   |
| **MEDIUM**  | Multiple dependent files, components, or agents   | Persistent planner-confirmed DAG; risk-adaptive plan review       |
| **HIGH**    | Architecture, API/schema/auth, migration, or risk | Persistent DAG, high or critic plan review, risk-triggered checks |

## Complex Plans Are Resumable

MEDIUM/HIGH tasks get a persistent plan ID and fresh `plan.yaml`. TRIVIAL/LOW tasks use only an ephemeral `execution_id` and no plan artifacts. Routine MEDIUM plans can proceed after deterministic validation; independent review is retained for explicit review requests, risk signals, or validation failures.

MEDIUM/HIGH tasks produce durable plan artifacts. The Orchestrator gathers material discovery first, then gives the Planner a bounded handoff so planning
does not repeat broad repository reads:

```text
docs/plan/{plan_id}/
  plan.yaml              # Task DAG, status, and minimal outputs
```

Task results use minimal role-specific JSON and are not stored as separate files. Detailed evidence stays in task-scoped artifacts; only compact, actionable context is relayed to dependent tasks.

Resume paused work with the exact plan ID:

```text
continue plan 20260714-api-rate-limiter
```

Continuation and extension require the exact persistent plan ID.

## After Successful Work

Gem Team can promote stable, reusable knowledge after final success. Promotion
is confidence-gated; task-local details stay in execution state.

- **Product decisions** -> `docs/PRD.yaml`
- **Technical conventions** -> `AGENTS.md`
- **Patterns & gotchas** -> Memory (repo/session/global)
- **Repeatable workflows** -> `.apm/skills/` (as `SKILL.md` playbooks; compiled into each target tool by APM)

## Practical Tips

- **Be specific**: clearer input -> better plans. Include error messages, file paths, and expected behavior.
- **Check plan artifacts** for MEDIUM/HIGH tasks: they show the full breakdown before execution starts.
- **Use plan IDs** to resume or extend MEDIUM/HIGH work across sessions.
- **Keep `apm_modules/` in `.gitignore`**: it's an install cache, not project code.
