---
title: Production Best Practices
description: Team workflows, version pinning, and operational guidelines.
navigation:
  icon: i-lucide-shield
---

## Project-Scoped Installs

Prefer project-scoped installs for teams: `apm.yml` and `apm.lock.yaml` make setup reproducible across environments and CI.

## Version Pinning

```bash
apm install 'mubaidr/gem-team#gem-team-v<version>' --target copilot
```

Gem Team release tags follow `gem-team-v<version>`. Pinning a tag makes the
selected source explicit in addition to the lockfile's commit and content hash.
Choose a published version from
[GitHub Releases](https://github.com/mubaidr/gem-team/releases).

## Updates and Lockfiles

Plain `apm install` is a reproducible sync: it restores versions recorded in
`apm.lock.yaml`. Update deliberately:

```bash
apm update mubaidr/gem-team --yes
apm update -g mubaidr/gem-team --yes
```

The first command updates a project installation; the second updates the global
user installation under `~/.apm/`. `apm self-update --check` checks for a newer
APM CLI without installing it.

## Housekeeping

- Keep `apm_modules/` out of git (add to `.gitignore`).
- Run `apm run check` before releases and in CI.
- Review generated files before committing large updates.

## Approval-Gated Operations

These require explicit approval before execution:

- Production deployment
- Infrastructure provisioning
- Data migration
- Destructive operations

## AGENTS.md vs. Plan Context

| Purpose                               | Location                                |
| ------------------------------------- | --------------------------------------- |
| Stable project rules and conventions  | `AGENTS.md`                             |
| Persistent MEDIUM/HIGH plan context   | `docs/plan/{plan_id}/`                  |
| Ephemeral TRIVIAL/LOW execution state | In memory, identified by `execution_id` |

## Agent result hygiene

Agent results are minimal and role-specific. Keep only status, failure classification, evidence
needed by the orchestrator, and actionable downstream handoffs. Do not add a universal `learn` or
per-criterion acceptance-results field. Store detailed logs and reports under task-scoped execution
artifacts and return their path or manifest when another task needs them. Ephemeral execution must
not read or write persistent plan artifacts.

## Next

[Configuration ->](/configuration) · [Workflow ->](/guide/workflow)
