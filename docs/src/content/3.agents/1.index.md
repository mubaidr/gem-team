---
title: Agent Team
description: Complete catalog of Gem Team's specialist agents.
navigation:
  icon: i-lucide-bot
---

## Agent Definitions

Agent definitions live in `.apm/agents/` and are routed by the Orchestrator.

## Orchestrator

Coordinates the Phase 0-4 workflow, delegates work, tracks state, and enforces verification gates. It classifies from supplied evidence and never executes specialist work directly.

## Core Agents

| Agent           | Role                                                                                                                                                                                                                   |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Researcher**  | Explores codebase patterns, dependencies, architecture, and docs. 5 modes (scan, deep, audit, trace, question) with budget controls. Runs directly for standalone research or when assigned by a persistent wave plan. |
| **Planner**     | Validates the contract and baseline; builds a minimal wave plan with routing, ordered waves, per-task handoffs, and risks; persists `docs/plan/{plan_id}/plan.yaml`; applies YAGNI/KISS.                               |
| **Implementer** | Implements features, fixes, and refactors using TDD (Red-Green-Refactor). Tests happy paths, invariants, boundaries, error paths, input variation, and state transitions. Bug-fix mode requires diagnosis.             |

## Quality & Review Agents

| Agent               | Role                                                                                                                                                                                                                 |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Reviewer**        | Reviews plans and implementations; provides read-only critic support for decision challenges; checks quality, security, contracts, compliance, and regressions.                                                      |
| **Debugger**        | Root-cause analysis, regression bisection, and verified error reproduction. Reproducible bugs get a minimal reproduction test before fix recommendations. Never implements fixes.                                    |
| **Browser Tester**  | E2E browser and UI flow checks; visual, accessibility, performance, network, and regression checks are conditional.                                                                                                  |
| **Code Simplifier** | Removes dead code, reduces cyclomatic complexity, consolidates duplicates, improves naming. Applies design smell patterns (Strategy, Interface Segregation, Layer Separation). Follows Chesterton's Fence principle. |

## Specialized Agents

| Agent             | Role                                                                                                                                |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| **DevOps**        | Infrastructure deployment, CI/CD pipelines, container management (Docker/K8s). Approval gates for prod. Health checks and rollback. |
| **Documentation** | Technical docs, READMEs, API docs, diagrams, walkthroughs. PRD authoring and maintenance.                                           |
| **Mobile Tester** | Mobile E2E with Detox, Maestro, Appium; scope-selected checks for gestures, lifecycle, push, performance, and device farms.         |
| **Skill Creator** | Extracts reusable `SKILL.md` files from high-confidence patterns. Creates scripts, references, and cross-linked assets.             |

## Reviewer routing

`gem-reviewer` uses three axes:

- `review_mode`: `standard`, `high`, or `critic` controls review intensity.
- `review_target`: `plan`, `task`, `code`, `decision`, `docs`, `config`, or `integration` selects the target.
- `review_scope`: `changed`, `affected`, or `full` limits evidence breadth.

Single bounded, low-risk work uses the direct specialist fast path. Other
Other TRIVIAL/LOW work uses ephemeral planning without planner or reviewer calls.
For MEDIUM/HIGH work, the Orchestrator supplies discovery and clarifications as `planning_context`; the Planner owns wave structure and per-task handoffs, while the Orchestrator owns workflow state, retries, approvals, review invocation, and replan limits. Replans include the immutable baseline, the exact current plan, and concrete findings.
For MEDIUM/HIGH plans, the Orchestrator first runs deterministic plan
validation. Routine MEDIUM plans with no risk signals or explicit review request
can proceed without an independent reviewer. A plan review uses full scope when
required: standard for an explicit review request or insufficient or
contradictory evidence, high for HIGH/high-risk work, or critic for
architecture, breaking-change, or cross-domain signals. A later integration
review uses high mode, integration target, and affected scope only when the
review can change the decision. Discussion is answered directly; requested
decision evaluation uses critic mode.

Critic mode is read-only. It evaluates assumptions, counterexamples, risks,
alternatives, reversibility, and decision blockers without implementing work,
mutating files, or claiming completion. The structured result includes a
`critic_verdict`, `challenges`, `alternatives`, and `decision_blockers`; every
reported record has evidence, impact, and action. There is no standalone
`gem-critic` agent.

Critic requests carry these objects under `handoff`:

```yaml
review_mode: critic
review_target: decision
review_scope: full
handoff:
  critic_subject:
    objective: string
    proposal: string
    constraints: string[]
    alternatives: string[]
    evidence: string[]
    decision_needed: string
  critic_context:
    audience: string
    time_horizon: string
    success_criteria: string[]
    known_unknowns: string[]
```

## Model Routing

The Orchestrator supports two configurable model tiers through
`model_routing` in `.gem-team.yaml`:

| Tier        | Agents                                                                                                        | Purpose                                                          |
| ----------- | ------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| **Premium** | Planner, Debugger, Reviewer                                                                                   | Reasoning-heavy planning, diagnosis, challenge, and verification |
| **Explore** | Researcher, Implementer, Browser Tester, Mobile Tester, DevOps, Documentation, Skill Creator, Code Simplifier | Fast exploration and bounded project work                        |

When enabled, the Orchestrator passes the configured tier model to each
delegated agent. The Orchestrator itself is not routed through these tiers.

| Agent Tier  | Recommended Model                                                     | Use Case                                               |
| :---------- | :-------------------------------------------------------------------- | :----------------------------------------------------- |
| **Explore** | Fast / flash (`gpt-5.6-luna`, `claude-3.5-haiku`, `gemini-2.5-flash`) | Implementation, docs, research, simple checks          |
| **Premium** | Strong / pro (`gpt-5.6-sol`, `claude-4`, `gemini-2.5-pro`)            | Planning, analysis, compliance, high-risk verification |

Configurable in `.gem-team.yaml`. Swap with equivalent models from your provider.

## Packaged Skills

Reusable, task-specific guidance lives separately from agent definitions in `.apm/skills/`:

| Skill                   | Purpose                                                                                      |
| :---------------------- | :------------------------------------------------------------------------------------------- |
| `gem-devops-guidelines` | Deployment strategies, CI/CD, containers, health checks, rollback, and production readiness. |

Skills are loaded for matching tasks and keep specialist agent definitions focused.
