---
title: Optimizations
description: Best practices for optimizing Gem Team's performance, bundle size, and runtime efficiency.
navigation:
  icon: i-lucide-zap
---

## Overview

Gem Team is designed with cost-consciousness and performance as core principles. This guide covers optimization strategies spanning agent execution, context management, build performance, and runtime efficiency.

## Agent Execution Optimization

### Model Routing

One of the most impactful optimizations is routing tasks to the right model. Gem Team supports model routing out of the box:

| Task Type          | Recommended Model Tier | Rationale                               |
| :----------------- | :--------------------- | :-------------------------------------- |
| Planning/Debugging | Strong (GPT-4, Claude) | Needs deep reasoning                    |
| Review/Critique    | Strong                 | Needs nuanced analysis                  |
| Implementation     | Standard               | Pattern-based, well-scoped              |
| Documentation      | Standard or Fast       | Template-driven, low reasoning overhead |
| Research           | Standard               | Search-heavy, less synthesis            |

Configure model routing in your `.gem-team.yaml`:

```yaml
orchestrator:
  model_routing:
    gem-planner: "claude-sonnet-4"
    gem-debugger: "claude-sonnet-4"
    gem-critic: "claude-sonnet-4"
    gem-reviewer: "claude-sonnet-4"
    gem-implementer: "gpt-4o-mini"
    gem-documentation-writer: "gpt-4o-mini"
    gem-researcher: "gpt-4o-mini"
```

### Output Hygiene

Agents are configured to minimize token waste:

- **Native tool flags**: Agents use `--quiet`, `--oneline`, `maxResults` to truncate output at source
- **Pipeline truncation**: `head`/`tail`/`grep` used only when native flags are insufficient
- **Context pruning**: The context envelope is filtered per-agent — each agent receives only the sections relevant to its task

### Researcher Budget Controls

The `gem-researcher` agent has configurable budgets to prevent runaway exploration:

```yaml
orchestrator:
  researcher:
    max_searches: 10 # Max search calls per task
    max_files_to_read: 25 # Max files read per task
    max_depth: 3 # Max exploration depth (follow-up searches)
```

## Context & Cache Optimization

### Context Envelope

The context envelope is a progressive cache that grows as work progresses. To keep it lean:

- **Filter per-agent**: Each agent only receives the sections it needs
- **Regular pruning**: Stale or superseded facts are evicted (90-day auto-eviction for unused cache items)
- **Confidence thresholds**: Facts are persisted only when confidence ≥ 0.85, with high-confidence items (≥ 0.90, stable, ≥ 3 uses) promoted to durable cache

### Memory Tier Management

| Tier        | Scope               | Eviction Policy                                 |
| :---------- | :------------------ | :---------------------------------------------- |
| **Repo**    | Workspace-scoped    | ~90 days without access                         |
| **Session** | Conversation-scoped | Cleared after conversation ends                 |
| **Global**  | User-scoped         | ~90 days without access, confidence-gated write |

## Build & CI/CD Optimization

### Dependency Caching

The CI workflow caches `node_modules` via `actions/setup-node` with `cache: npm`:

```yaml
- uses: actions/setup-node@v4
  with:
    node-version: "22"
    cache: npm
    cache-dependency-path: docs/src/package-lock.json
```

### Prerendering Strategy

The docs site uses static site generation (SSG) with prerendering for optimal CDN delivery:

```ts [nuxt.config.ts]
nitro: {
  prerender: {
    routes: ["/"],
    crawlLinks: true,
  },
}
```

This ensures:

- HTML is pre-built at deployment time
- No server-side rendering overhead at request time
- Maximum cacheability on CDN edge nodes

## Bundle Size Optimization

### Tree-shaking & Auto-imports

Nuxt's auto-import system tree-shakes unused exports, ensuring only used components and composables are included in the final bundle.

### Image Optimization

[Nuxt Image](https://image.nuxt.com/) is configured to:

- Automatically convert images to WebP/AVIF formats
- Generate responsive sizes
- Support lazy loading with `loading="lazy"` and `fetchpriority="low"`
- Use `preload` with `fetchPriority: "high"` for critical LCP images

```html
<NuxtImg
  src="/hero-banner.jpg"
  format="webp"
  :preload="{ fetchPriority: 'high' }"
  loading="eager"
  width="200"
  height="100"
/>
```

### Lazy Hydration

Components that don't need immediate interactivity can defer hydration:

```html
<LazyMyComponent hydrate-on-visible />
```

This improves Time-to-Interactive (TTI) by deferring JavaScript execution for below-the-fold content.

## Measuring & Monitoring

### Built-in Tools

- **Nuxt DevTools**: Timeline, Assets, Render Tree, and Inspect panels for performance analysis
- **`nuxi analyze`**: Visualize bundle composition to identify large dependencies
- **Chrome DevTools**: Performance panel for LCP/CLS/INP metrics; Lighthouse for comprehensive audit

### Continuous Monitoring

Enable the link checker to catch broken links that degrade SEO and user experience:

```ts [nuxt.config.ts]
modules: [
  "@nuxtjs/seo",
],
```

> The built-in SEO module includes link checking, sitemap generation, and robots.txt management.

## Configuration Reference

```yaml
# .gem-team.yaml optimization settings
orchestrator:
  max_concurrent_agents: 2 # Parallel agent execution limit
  git_commit_on_gate_pass: true # Auto-commit after successful wave gates
  model_routing:
    # ... per-agent model assignments ...
  researcher:
    max_searches: 10
    max_files_to_read: 25
    max_depth: 3
```

## Related

- [Core Concepts →](/guide/core-concepts) — architecture and knowledge layers
- [Workflow →](/guide/workflow) — the execution pipeline in detail
- [Configuration →](/configuration) — all `.gem-team.yaml` options
